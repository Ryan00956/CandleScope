# Backtest Python First N6 Studio 入口（2026-08-15）

## 结论

状态：`PYTHON_STUDIO_PRODUCT_PATH_PASS`（flag-off 默认）。

`VITE_BACKTEST_PYTHON_STRATEGY_ENABLED` 默认关闭。开启后展示 Python 卡片，文案写明
Python 决策、Host 生成订单/成交/报告。本环境未启动完整浏览器验收；静态入口与 flag
映射测试覆盖真实 shipped 函数。
